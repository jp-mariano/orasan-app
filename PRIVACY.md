# Privacy Policy

Orasan collects minimal data:

- **Core**: `xUserId` (X ID), `reminders` (message, duration).
- **Premium**: Opt-in analytics (TBD in `/ee`).

Data stays on our servers (Supabase, Vercel) or Akash (AI parsing, no PII). Audit our code to verify.
